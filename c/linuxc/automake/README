setup 1:

	autoscan
setup 2:
	mv configure.scan configure.ac
setup 3:
	add to configure.ac :
		AM_INIT_AUTOMAKE
		AC_PROG_LIBTOOL
	touch file:(error will give the file name ,use touch create it)
		touch NEWS
		touch README
		touch ChangeLog
		
setup 4:
	autoreconf --install
setup 5:
	make
